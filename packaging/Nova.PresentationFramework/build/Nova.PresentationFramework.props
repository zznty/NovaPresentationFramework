<Project>
  <!-- Thin re-import of buildTransitive, guarded so NuGet's direct-dependency
       build/ import never double-executes the props. -->
  <Import Project="..\buildTransitive\Nova.PresentationFramework.props" Condition="'$(NovaPresentationFrameworkPropsImported)' != 'true'" />
</Project>
